{
  "name": "template-instantiation-prollyfill",
  "version": "0.0.4",
  "description": "A prollyfill for HTML template instantiation",
  "main": "template-instantiation.js",
  "files": [
    "lib/*"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "bundle": "rollup -c ./rollup.config.js",
    "prepublishOnly": "npm run build && npm run bundle"
  },
  "author": "The Polymer Authors",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@polymer/ristretto": "^0.3.1",
    "@types/chai": "^4.0.10",
    "chai": "^4.1.2",
    "rollup": "^0.56.3",
    "typescript": "^2.6.2"
  }
}
